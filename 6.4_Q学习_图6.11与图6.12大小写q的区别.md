# 6.4 Q学习：图 6-11 用小写 q、图 6-12 用大写 Q 的原因

两张图分别画的是**同一个东西的两个版本**：图 6-11 是 Q 函数**贝尔曼方程**的回溯线形图，图 6-12 是它的**“采样版”——也就是 SARSA** 的回溯线形图。大小写的差别不是笔误，而是本书（以及强化学习领域）一个**严格的记号约定**。这里其实涉及**两组**大小写差别，分开讲。

---

## 一、核心约定：小写 = 真值/理论，大写 = 估计值/采样

### 1. 函数符号 `q` vs `Q`

| 符号 | 含义 | 出现在 |
|------|------|--------|
| **小写 $q_\pi(s,a)$** | **真值**：由贝尔曼方程**精确定义**的、理论上的 Q 函数。是一个数学上确定存在的“标准答案” | 图 6-11 |
| **大写 $Q(S_t,A_t)$** | **估计值**：智能体手里那张不断被更新的 Q 表，是对真值 $q_\pi$ 的**近似** | 图 6-12 |

> 打个比方：
> - 小写 $q_\pi$ 就像一道题的**标准答案**——它客观存在、唯一确定，写在“出题老师”手里。
> - 大写 $Q$ 就像**学生草稿纸上的答案**——一开始是瞎填的，通过一遍遍练习（更新）慢慢逼近标准答案。

### 2. 状态/动作符号 `s, a` vs `S_t, A_t`

| 符号 | 含义 |
|------|------|
| **小写 $s, a$** | **普通变量（占位符）**，表示“任意一个状态、任意一个动作”，用来写一条对所有情况都成立的等式 |
| **大写 $S_t, A_t$** | **随机变量**，表示在时刻 $t$ 智能体**实际采样**到的那个具体状态、动作 |

---

## 二、为什么图 6-11 用小写

图 6-11 画的是**贝尔曼方程**：

$$
q_\pi(s,a) = \sum_{s'} p(s'\mid s,a)\Big\{\, r(s,a,s') + \gamma \sum_{a'} \pi(a'\mid s')\, q_\pi(s',a') \,\Big\}
$$

注意它的两个特征（正是书里强调的两点）：

1. 对下一步**所有可能的状态 $s'$** 求和（$\sum_{s'}$）；
2. 对下一步**所有可能的动作 $a'$** 求和（$\sum_{a'}$）。

也就是说，**它把所有分支一个不漏地全考虑了**，算的是精确的数学期望。

- 因为是“穷举所有情况的理论关系”，没有任何采样、没有随机性，所以状态动作用**小写 $s,a$**（普通变量）。
- 因为它定义的是那个**精确的标准答案**，所以函数用**小写 $q_\pi$**（真值）。

图 6-11 里那一根根分叉的线，画的就是“从 $(s,a)$ 出发，扇形展开到**每一个**可能的 $s'$，再扇形展开到**每一个**可能的 $a'$”。

---

## 三、为什么图 6-12 用大写

图 6-12 画的是 SARSA，是贝尔曼方程的**“采样版”**。所谓采样版，就是**不再穷举所有分支，而是只随机走一条路**：

1. 按状态迁移概率 $p(s'\mid s,a)$ 只**采样出一个**下一状态 $S_{t+1}$；
2. 按策略 $\pi(a'\mid s')$ 只**采样出一个**下一动作 $A_{t+1}$。

于是 TD 目标变成 $R_t + \gamma Q(S_{t+1}, A_{t+1})$，更新式（同策略 SARSA）是：

$$
Q(S_t,A_t) \leftarrow Q(S_t,A_t) + \alpha\Big\{ R_t + \gamma Q(S_{t+1},A_{t+1}) - Q(S_t,A_t) \Big\}
$$

对应到大小写：

- 因为 $S_t, A_t, S_{t+1}, A_{t+1}$ 都是**实际采样得到的具体随机变量**（这一局到底走到哪、选了啥，是掷骰子掷出来的），所以用**大写 $S_t, A_t$**。
- 因为这是**算法在实际运行、用估计值去逼近真值**——左边的 $Q$ 是被更新的草稿、右边也是当前草稿值，整个过程在“练习”而非“查标准答案”，所以函数用**大写 $Q$**。

图 6-12 里不再是扇形展开的一大片分叉，而是**一条细线**：从 $(S_t,A_t)$ 采样到 $S_{t+1}$，再采样到 $A_{t+1}$——这就是“采样版”和“全展开版”在图上的直观区别。

---

## 四、一句话总结

> **图 6-11（贝尔曼方程）= 标准答案 + 穷举所有分支** → 用小写 $q_\pi(s,a)$（真值 + 普通变量）；
> **图 6-12（SARSA）= 草稿估计 + 只走一条采样路径** → 用大写 $Q(S_t,A_t)$（估计值 + 随机变量）。

记住这条主线就不会搞混：**小写代表“理论上精确的、穷举的真值”，大写代表“实际采样得到、靠更新逼近的估计值”。** SARSA（图 6-12）正是用大写的、采样的方式，去逼近图 6-11 里小写的、理论的那个标准答案。

> 顺带一提：紧接着的图 6-13（贝尔曼**最优**方程）同样用小写 $q_*(s,a)$，而图 6-14（Q 学习，它的采样版）用大写 $Q(S_t,A_t)$——和这里的 6-11 / 6-12 是完全一样的对应关系，只是把“普通贝尔曼方程→SARSA”换成了“贝尔曼最优方程→Q 学习”。
