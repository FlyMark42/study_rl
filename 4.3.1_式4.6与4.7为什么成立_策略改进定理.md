# 4.3.1 式4.6 / 4.7 为什么成立 —— 策略改进定理

> 对应参考资料原文（`全文.txt` 第2971–3016行）：
> "式4.4是最优策略的式子，在这里我们考虑将式4.4应用于一些确定性的策略 μ……来看一下下面的式子。"
> "通过式4.6或式4.7更新策略的做法叫作'贪婪化'。"
> 书中原文注明：**"这里只说明事实，而省略了证明。"**

---

## 一、先还原四个式子

**式4.4 / 4.5（最优策略，前面已学）**，用的是**最优**价值函数 `q*`、`v*`：

$$\mu^*(s) = \arg\max_a q^*(s,a) \tag{4.4}$$
$$= \arg\max_a \sum_{s'} p(s'|s,a)\{r(s,a,s') + \gamma v^*(s')\} \tag{4.5}$$

**式4.6 / 4.7（本节新内容）**，把上面的思路套到**某个当前策略 μ** 上，用的是**当前策略**的价值函数 `q_μ`、`v_μ`：

$$\mu'(s) = \arg\max_a q_\mu(s,a) \tag{4.6}$$
$$= \arg\max_a \sum_{s'} p(s'|s,a)\{r(s,a,s') + \gamma v_\mu(s')\} \tag{4.7}$$

- 当前策略：`μ(s)`
- 当前策略的价值函数：`q_μ(s)`、`v_μ(s)`
- 更新得到的新策略：`μ'(s)`

---

## 二、问题拆成两层

### 第一层：为什么 式4.6 = 式4.7？（两行为什么能划等号）

这纯粹是**行动价值函数 q 的定义（贝尔曼方程）**，不需要额外证明：

$$q_\mu(s,a) = \sum_{s'} p(s'|s,a)\{r(s,a,s') + \gamma v_\mu(s')\}$$

含义：**在状态 s 先强制走一步动作 a，之后再按策略 μ 走**，能拿到的期望回报。

把这个定义**代入**式4.6里的 `q_μ(s,a)`，就变成了式4.7。所以两行是**同一个东西的两种写法**：

- **式4.6**：如果手头已有 q 表，直接查 q 挑最大。
- **式4.7**：如果只有 v 表和环境模型 `p`、`r`，就用展开式现算。

> 一句话：4.6→4.7 只是"把 q 用 v 展开"，靠的是 q 的定义。

### 第二层：为什么"用当前策略的价值做 argmax"是有效的？

注意 4.4/4.5 用的是**最优**价值 `v*`（可望不可即，先有鸡还是先有蛋）；而 4.6/4.7 聪明在——**换成当前策略能算出来的 `v_μ`**。合理性来自**策略改进定理（policy improvement theorem）**，见下节直观讲解。

---

## 三、策略改进定理为什么"只会更好"（直观讲解）

### 核心直觉："只改第一步试试"

在状态 s 做一个**很克制的改动**：

> **只在第一步**改成"选让 `q_μ(s,a)` 最大的动作 a"，**从第二步起，仍按老策略 μ 走。**

这个"只改第一步"的价值恰好是 `max_a q_μ(s,a)`，而老策略在 s 的价值是 `v_μ(s)`，两者一比：

$$\max_a q_\mu(s,a) \;\ge\; q_\mu(s, \mu(s)) = v_\mu(s)$$

- `q_μ(s, μ(s))`：第一步就按老策略选 → 等于 `v_μ(s)`（啥也没改）。
- `max_a`：取最大值 → **至少不会比其中的老动作差**。

所以：**改第一步这个动作，价值 ≥ 原来。最坏打平，绝不会变差。**

### 从"只改一步"到"每步都改"：滚雪球

新策略 `μ'` 是**每个状态都贪婪改**，会不会改多了砸锅？不会，靠**层层递推**：

1. 第一步用 μ' 的新动作 → 已经 ≥ 老的。
2. 走到下一状态 s'，本说好"之后按 μ 走"，但 μ' 在 s' 又贪婪，**同理**改第一步也 ≥ 按 μ。
3. 好处一站一站往后叠加，**每步替换只增不减**。

展开到底得到：

$$v_{\mu'}(s) \ge v_\mu(s) \quad(\text{对所有状态 } s)$$

> 每一步单独看都"不吃亏"，整条路径加起来自然也"不吃亏"。

### 具体数字例子

在状态 s 有两个动作，老策略 μ 习惯选"左"，已算好当前行动价值：

| 在 s 选的动作 | `q_μ(s, a)` |
|------|------|
| 左（老策略选的）| 5 |
| 右 | **8** |

- 老策略价值：`v_μ(s) = q_μ(s, 左) = 5`
- 贪婪化选"右"，价值 = **8** ≥ 5，更好。

"右"的 8 含义是"**在 s 先走右，之后仍按老策略 μ 走**"——**没对未来做任何乐观假设**，未来照旧用老策略，仅把当下换成更划算的，结果就已经涨了。这正是它"稳赚不赔"的原因。

### 什么时候"打平"？——顺便理解收敛

如果在**每个状态**贪婪选出的动作**恰好就是老策略已在选的**（`max` 选不出更好的），则：

$$v_{\mu'}(s) = v_\mu(s)$$

策略**不再变化**，此时 μ 满足贝尔曼最优方程，说明它**已是最优策略**——这也解释了"策略迭代法"反复"评估→贪婪化"最终会停下来。

---

## 四、一句话总结

> **式4.6=式4.7 靠的是 q 的定义（把 q 展开成 v）；而"用 v_μ 做 argmax 得到更好的 μ'"靠的是策略改进定理——只改第一步动作、未来仍用老策略兜底，所以每步稳赚不赔，层层累加后整个策略只会更好，直到再也改不动即为最优。**
