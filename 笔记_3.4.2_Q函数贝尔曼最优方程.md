# 3.4.2 Q 函数的贝尔曼最优方程：3.17 如何推导出 3.18

> 出处：《深度学习入门4：强化学习》第 3 章，书 p.72–73（PDF_PAGE 90–91）

---

## 一、先理解 Q 函数（行动价值函数）是什么

回顾两个「价值」的区别，这是理解全部的关键：

| 符号 | 名字 | 含义 |
|------|------|------|
| `v_π(s)` | 状态价值函数 | 在状态 s，**按策略 π 去选动作**，之后能拿到的期望回报 |
| `q_π(s, a)` | 行动价值函数（Q 函数） | 在状态 s，**先强行执行动作 a**（这一步不管策略），从下一步起再按 π，能拿到的期望回报 |

一句话：**Q 函数比 V 函数多固定了「当前这一步做什么动作」**。它回答的是「在 s 状态、我先做 a 这个动作，到底值多少？」

---

## 二、起点：Q 函数的贝尔曼方程（式 3.17 的来源）

Q 函数也有自己的贝尔曼方程（书 p.70，对任意策略 π 都成立），标准写法是：

```
q_π(s,a) = Σ_s' p(s'|s,a) { r(s,a,s') + γ · Σ_a' π(a'|s') · q_π(s',a') }
```

逐块拆开看（通俗版）：

- `Σ_s' p(s'|s,a)` —— 我在 s 做了动作 a，环境**可能**把我送到不同的下一状态 s′，按概率加权。
- `r(s,a,s')` —— 这一步**立刻拿到的奖励**。
- `γ` —— 折扣率，未来的钱打折。
- `Σ_a' π(a'|s') · q_π(s',a')` —— **到了下一状态 s′ 后，按策略 π 去选下一个动作 a′**，得到的未来价值的平均。

⚠️ 注意最后这一项：因为用的是**一般策略 π**，到了 s′ 还要「按 π 的概率分布去抽动作 a′」，所以有个 `Σ_a' π(a'|s')`（对所有可能的下一个动作加权平均）。

**式 3.17** 就是把上式里的策略换成**最优策略 π\***（书里写「代入最优策略」）：

```
q_*(s,a) = Σ_s' p(s'|s,a) { r(s,a,s') + γ · Σ_a' π_*(a'|s') · q_*(s',a') }   ……(3.17)
```

此时未来那一项里还**残留着 `Σ_a' π_*(a'|s')`**，看着还很复杂。下一步就是把它干掉。

---

## 三、关键一步：3.17 → 3.18 怎么推出来

原文的原话是：

> 「这里 π\* 是最优策略，所以可以用 max 算子来简化。具体来说就是将 `Σ_a' π_*(a'|s')…` 部分替换为 `max_a'`。」

**核心就一句：最优策略不是「按概率乱挑」，而是「永远挑最好的那个」。**

### 为什么「Σ 加权平均」能变成「max 取最大」？

回看 3.4.1 节图 3-12 的例子（书 p.72）：在某状态有 3 个候选动作，它们的价值分别是 **−2、0、4**。

- 如果是**普通随机策略**：可能 30% 选 −2、30% 选 0、40% 选 4，要算**加权平均**（这就是 `Σ π(a')·q(s',a')`）。
- 但**最优策略**追求回报最大，它**没有任何理由**去碰 −2 或 0，必然 **100% 选 4**。

所以对最优策略来说：

```
Σ_a' π_*(a'|s') · q_*(s',a')  =  max_a' q_*(s',a')
```

- **左边**：按最优策略的概率加权平均。
- **右边**：直接取最大值。

两者相等——因为最优策略把全部概率（100%）都压在了那个**价值最大的动作**上，加权平均自然就等于最大值本身。这就是 `Σ_a' π_*(a'|s')` 被**替换成 `max_a'`** 的原因。

### 代入后得到式 3.18

把上面这个等式代回 3.17 的对应位置：

```
q_*(s,a) = Σ_s' p(s'|s,a) { r(s,a,s') + γ · max_a' q_*(s',a') }   ……(3.18)
```

这就是 **Q 函数的贝尔曼最优方程**。

---

## 四、对比一下，体会简化在哪

```
3.17（残留 Σ_a' π_*）：
  q_*(s,a) = Σ_s' p(s'|s,a) { r + γ · [ Σ_a' π_*(a'|s') · q_*(s',a') ] }
                                        └── 要先知道最优策略 π_* 才能算 ──┘

3.18（换成 max）：
  q_*(s,a) = Σ_s' p(s'|s,a) { r + γ · [ max_a' q_*(s',a') ] }
                                        └── 直接取最大，不需要 π_* ──┘
```

**这一步简化的真正意义：**

- 3.17 里你得**先知道最优策略 π\* 长什么样**，才能算 `Σ π_* q`——可这正是我们最终要求的东西，等于「鸡生蛋、蛋生鸡」。
- 3.18 用 `max` 把 π\* 彻底消掉了：**只要会取最大值，方程里就只剩 q\* 自己**。于是它变成一个**只关于 q\* 的自洽方程**，可以直接迭代求解（这正是后面第 6 章 Q 学习的根基）。

---

## 五、一句话总结

> **3.17 → 3.18 的全部秘密**：最优策略总是 100% 选价值最大的动作，所以「对下一动作 a′ 按策略求加权平均」`Σ_a' π_*(a'|s')·q_*` 就等于「直接取最大」`max_a' q_*`。把前者换成后者，最优策略 π\* 就从方程里消失了，得到只含 max 的贝尔曼最优方程 3.18。

📌 它和上一节 V 函数版（3.16）完全对称：

- V 的 max 在状态 s 的**当前动作**上取（`max_a`）；
- Q 的 max 在下一状态 s′ 的**下一动作**上取（`max_a'`）。

原因：Q 已经把「当前动作 a」固定住了，只能对未来的动作取最大。

---

### 附：本节涉及的公式速查

| 编号 | 公式 |
|------|------|
| Q 贝尔曼方程（任意 π） | `q_π(s,a) = Σ_s' p(s'|s,a){ r + γ Σ_a' π(a'|s') q_π(s',a') }` |
| 3.16（V 贝尔曼最优方程） | `v_*(s) = max_a Σ_s' p(s'|s,a){ r + γ v_*(s') }` |
| 3.17（代入最优策略） | `q_*(s,a) = Σ_s' p(s'|s,a){ r + γ Σ_a' π_*(a'|s') q_*(s',a') }` |
| 3.18（Q 贝尔曼最优方程） | `q_*(s,a) = Σ_s' p(s'|s,a){ r + γ max_a' q_*(s',a') }` |
