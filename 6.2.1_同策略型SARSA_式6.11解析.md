# 式6.11 与「Q 更新了 → 状态 S_t 的策略可能会改变」的理解

> 对应参考资料《深度学习入门4：强化学习》6.2.1 节（同策略型的 SARSA），p.161–162。

## 1. 式6.11 在说什么

式6.11 描述的是：在状态 $S_t$ 下，智能体**怎么选择行动**（即策略 π）。它是经典的 **ε-greedy（ε-贪婪）** 算法：

$$
\pi(a \mid S_t) =
\begin{cases}
\text{argmax}_a\, Q(S_t, a) & （以\ 1-\varepsilon\ 的概率）\\[4pt]
\text{随机行动} & （以\ \varepsilon\ 的概率）
\end{cases}
$$

用大白话说：

- **大多数时候（$1-\varepsilon$，比如 90%）** → 选当前看起来「最划算」的行动（也就是 Q 值最大的那个行动），这叫**贪婪行动（greedy）**。
- **偶尔（$\varepsilon$，比如 10%）** → 故意随机走一步，这叫**探索（exploration）**。

> **为什么要故意随机走？**
> 如果永远只选「当前认为最好的」行动，那么有些还没试过的路，哪怕它其实更好，也永远发现不了。所以留 10% 去「试试别的路」。这正是同策略型（on-policy）中「改进阶段不能完全贪婪化」的原因（参考资料 p.162）。

---

## 2. 核心疑问：「Q 更新了，所以 S_t 的策略可能会改变」是什么意思？

关键点是：**策略 π 是由 Q 自动推导出来的**。想通这一点，全部就串起来了。

仔细看式6.11，贪婪部分是 $\text{argmax}_a Q(S_t, a)$，意思是「**在 $S_t$ 选 Q 值最大的那个行动**」。

那么——

> **如果 Q 值变了，「哪个行动最大」也可能跟着变。**
> 于是选出来的行动（也就是策略）也就变了。

**策略不是独立存在的，它是 Q 的「影子」（依附于 Q）**。Q 一动，影子就跟着动。

---

## 3. 具体例子：网格世界里的一个格子

假设你在状态 $S_t$（某个格子），能选 4 个行动【上、下、左、右】。

### 更新「前」的 Q 值

| 行动 | Q 值 |
|------|------|
| 上 | 3.0 |
| 下 | 5.0 ← **最大** |
| 左 | 2.0 |
| 右 | 4.0 |

现在「下」最大（5.0），所以贪婪行动是**「下」**。
→ 策略是「90% 选下，10% 随机」。

### 此时发生 TD 更新（式6.10）

假设你实际往「右」走了一步，发现后面能拿到很大的奖励。套用式6.10：

$$
Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha\{R_{t+1} + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t)\}
$$

这使得 $Q(S_t, \text{右})$ 从 **4.0 → 6.0**。

### 更新「后」的 Q 值

| 行动 | Q 值 |
|------|------|
| 上 | 3.0 |
| 下 | 5.0 |
| 左 | 2.0 |
| 右 | **6.0 ← 变成最大了！** |

于是 $\text{argmax}$ 的结果从「下」变成了**「右」**。
→ 策略也自动更新为「90% 选**右**，10% 随机」。

**这正是「$Q(S_t, A_t)$ 已更新，因此状态 $S_t$ 的策略改变了」的含义。**

---

## 4. 为什么是「可能会」改变，而不是「一定」改变

因为 **Q 更新了，但最大的行动不一定换人**。

比如 $Q(S_t, \text{右})$ 只从 4.0 涨到 4.5：

| 行动 | Q 值 |
|------|------|
| 下 | 5.0 ← **仍然最大** |
| 右 | 4.5 |

最大的还是「下」。这时候 Q 虽然更新了，但**策略没变**。

所以书里用了谨慎的说法——「**可能会**改变」。

---

## 5. 用代码验证（sarsa.py 的 `update` 方法）

参考资料 `ch06/sarsa.py` 的第 39～40 行，正好就是这两步：

```python
# 式6.10：更新 Q（评估）
self.Q[state, action] += (target - self.Q[state, action]) * self.alpha

# 式6.11：用更新后的 Q 重新生成策略 π（改进）
self.pi[state] = greedy_probs(self.Q, state, self.epsilon)
```

- **第 39 行**：更新 Q 值（评估阶段）
- **第 40 行**：**用新的 Q**，把这个状态的策略 `pi[state]` 重新算一遍（改进阶段）

第 40 行的 `greedy_probs` 就是式6.11本身——「给 Q 最大的行动分配 $1-\varepsilon$ 概率，剩下的概率分给探索」。**正因为 Q 变了，所以每次都要重新生成 π。**

---

## 总结（一句话）

| 术语 | 含义 |
|------|------|
| 式6.10 | 更新 Q（**评估**） |
| 式6.11 | 用更新后的 Q 生成策略（**改进**，即 ε-greedy） |
| 「策略可能会改变」 | 策略由 $\text{argmax}_a Q(S_t,a)$ 决定，所以**当 Q 最大值对应的行动换人时**，策略就变；不换人就不变。 |

SARSA 就是不断重复 **「评估（式6.10）→ 改进（式6.11）」**，让策略一步步逼近最优。
