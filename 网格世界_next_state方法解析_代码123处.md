# 网格世界 next_state 方法解析（代码①②③处）

> 对应参考资料原文：
> "代码①处，移动目的地可以被计算出来，此时暂时忽略墙和网格世界的边界。然后，代码②处可以判断智能代理是否已经移动到了边界外或墙上。如果是不能移动的目的地，则 `next_state = state`。另外，在这个任务中，状态迁移是确定性的，所以代码③处会返回下一个状态。"
>
> 代码位置：`common/gridworld.py` 的 `next_state` 方法。

---

## 一、背景：网格世界长什么样

```
reward_map = [[0,    0,    0,   1.0],   ← 第0行
              [0,   墙,    0,  -1.0],   ← 第1行
              [0,    0,    0,    0]]     ← 第2行
```

- 状态用 `(y, x)` 表示，即 `(行号, 列号)`。
- 尺寸：`height=3`（3行），`width=4`（4列）。
- 墙的位置：`wall_state = (1, 1)`。

---

## 二、完整代码与①②③标注

```python
def next_state(self, state, action):
    action_move_map = [(-1, 0), (1, 0), (0, -1), (0, 1)]
    move = action_move_map[action]
    next_state = (state[0] + move[0], state[1] + move[1])          # ← 代码①
    ny, nx = next_state

    if nx < 0 or nx >= self.width or ny < 0 or ny >= self.height:  # ← 代码②（撞边界）
        next_state = state
    elif next_state == self.wall_state:                            # ← 代码②（撞墙）
        next_state = state

    return next_state                                             # ← 代码③
```

---

## 三、逐处讲解

### 代码① —— 先算出"理想中"的目的地（暂时忽略墙和边界）

`action_move_map` 是**动作 → 坐标变化量**的对照表：

| 动作 | 含义 | move | 效果 |
|------|------|------|------|
| 0 | UP（上）  | (-1, 0) | 行号 -1 |
| 1 | DOWN（下）| (1, 0)  | 行号 +1 |
| 2 | LEFT（左）| (0, -1) | 列号 -1 |
| 3 | RIGHT（右）| (0, 1) | 列号 +1 |

例：智能体在 `(0, 0)`（左上角）想往上走：

```python
next_state = (0 + (-1), 0 + 0) = (-1, 0)
```

这一步**不管合不合理**，纯粹按方向加减，先算出来再说。所以说"**暂时忽略墙和边界**"。

### 代码② —— 检查目的地是否非法，非法就"待在原地"

刚才算出的 `(-1, 0)`，行号是 `-1`，已经跑出网格外了。于是：

- **第一个 `if`（撞边界）**：判断 `nx/ny` 是否越界。`ny = -1 < 0` 成立 → `next_state = state`，即**退回原地 `(0,0)`**。
- **第二个 `elif`（撞墙）**：如果算出的目的地正好是墙 `(1,1)`，同样 `next_state = state` 退回原地。

这就是原文那句"**如果是不能移动的目的地，则 `next_state = state`**"——撞墙或出界，就等于没动。

### 代码③ —— 直接返回下一个状态

```python
return next_state
```

因为"**状态迁移是确定性的**"：同一个状态 + 同一个动作，结果**永远唯一、不带随机性**（不像有些环境，往右走有 80% 到右边、20% 滑到别处）。所以不需要返回概率分布，**直接返回那唯一的 `next_state` 即可**。

---

## 四、一句话总结

> **①先无脑按方向算目的地 → ②发现出界或撞墙就退回原地 → ③因为结果唯一，直接返回。**
