# 式 4.2 为何成立？——贝尔曼方程如何变形为"更新式"

> 出处：《深度学习入门 4：强化学习》第 4 章 动态规划法，4.1 节（第 80–81 页）
>
> 原文：贝尔曼方程为许多强化学习算法提供了重要的基础。使用 DP 的方法也是从贝尔曼方程衍生出来的，其思路是将贝尔曼方程变形为"更新式"。

---

## 一、先看清两个式子的关系

**式 4.1（贝尔曼方程）**——第 3 章严格推导出的**恒等式**，对真正的价值函数 $v_\pi$ 精确成立：

$$v_\pi(s) = \sum_{a,s'} \pi(a|s)\, p(s'|s,a)\,\big\{\,r(s,a,s') + \gamma\, v_\pi(s')\,\big\} \tag{4.1}$$

**式 4.2（更新式）**——把等号两边换成第 $k+1$ 次和第 $k$ 次的**估计值** $V$：

$$V_{k+1}(s) = \sum_{a,s'} \pi(a|s)\, p(s'|s,a)\,\big\{\,r(s,a,s') + \gamma\, V_k(s')\,\big\} \tag{4.2}$$

- $V_{k+1}(s)$：第 $k+1$ 次更新后的价值函数（**推测值**）
- $V_k(s)$：第 $k$ 次更新后的价值函数（**推测值**）
- 因为是推测值、和真值 $v_\pi(s)$ 不同，所以用**大写 $V$** 表示。

> **关键点**：式 4.2 **不是**像式 4.1 那样从定义"推导"出的恒等式，而是**一种解方程的方法**——把"方程"改造成"迭代规则"。它成立（可用）的依据是：**这样反复迭代一定收敛到 $v_\pi$**。下面把这件事讲透。

---

## 二、核心思想：把"方程"变成"反复逼近的规则"

贝尔曼方程本质上是一个形如 $v = f(v)$ 的方程：未知数是价值函数 $v$，$f$ 是式 4.1 右边那一整串运算。真正的 $v_\pi$ 就是让等式成立的解，数学上叫**不动点**（fixed point）——代进去，出来的还是它自己。

解这种方程有个经典套路（**不动点迭代**）：随便猜一个初始值 $V_0$，然后反复做

$$V_{k+1} = f(V_k)$$

这正是式 4.2 干的事。书里所谓"将贝尔曼方程变形为更新式"，就是把**等号**从"必须成立的约束"改读成"用右边算出左边的下一次估计"。

---

## 三、一个一年级就能算的示例

解方程 $x = 0.5x + 1$（真解显然是 $x = 2$）。

用同样的套路：从 $x_0 = 0$ 出发，反复算 $x_{k+1} = 0.5\,x_k + 1$：

| 迭代次数 $k$ | $x_k$ | 与真解 2 的距离 |
|:---:|:---:|:---:|
| 0 | 0      | 2      |
| 1 | 1      | 1      |
| 2 | 1.5    | 0.5    |
| 3 | 1.75   | 0.25   |
| 4 | 1.875  | 0.125  |
| … | …      | …      |
| ∞ | 2      | 0      |

每迭代一次，与真解的距离就缩小到原来的 **0.5 倍**，所以必然收敛到 2。

**式 4.2 就是把这个套路用在贝尔曼方程上**。而上面那个决定收敛快慢的"0.5"，在式 4.2 里对应的正是**折现率 $\gamma$**。

---

## 四、为什么一定收敛？（收敛性的直觉证明）

把式 4.2 减去式 4.1（对同一个 $s$，两式的即时奖励项 $r(s,a,s')$ 完全相同，相减消掉）：

$$V_{k+1}(s) - v_\pi(s) = \gamma \sum_{a,s'} \pi(a|s)\,p(s'|s,a)\,\big\{\,V_k(s') - v_\pi(s')\,\big\}$$

右边是对各状态误差 $\big(V_k(s') - v_\pi(s')\big)$ 的**加权平均**（权重 $\pi(a|s)\,p(s'|s,a)$ 全部加起来等于 1），再乘上 $\gamma$。而"加权平均"不会超过其中的最大值，于是：

$$\max_s \big|V_{k+1}(s) - v_\pi(s)\big| \;\le\; \gamma \cdot \max_s \big|V_k(s) - v_\pi(s)\big|$$

**含义**：每更新一轮，最大误差至少缩小到原来的 $\gamma$ 倍。只要 $\gamma < 1$，误差就像 $\gamma^k$ 一样**指数级衰减到 0**，$V_k$ 最终收敛到真正的 $v_\pi$。

而且——**无论初始值 $V_0$ 怎么设都收敛**，所以书里说全部初始化为 0 就行。

> 这种"每次操作都把距离压缩 $\gamma$ 倍"的性质，数学上叫**压缩映射**（contraction mapping），收敛性由**不动点定理**（巴拿赫不动点定理）保证。这正是书中"根据证明，基于式 4.2 的重复更新会收敛……更多信息请参见参考文献 [5]"所指的内容。

---

## 五、这就是"自举法"（bootstrapping）

式 4.2 的特点是：用"下一个可能状态的价值 $V_k(s')$"来更新"当前状态的价值 $V_{k+1}(s)$"——**用一个估计值去改进另一个估计值**。这个过程叫**自举法**（bootstrapping）。

> bootstrapping 本指穿鞋时用手指挑起鞋背上的那个"环"，引申为**不靠外部帮助、只靠自己一步步改进**的过程。这里没有真值可参照，就拿自己上一轮的估计当参照，越迭代越准。

具体算法（**迭代策略评估**，iterative policy evaluation）：

1. 给 $V_0(s)$ 设初始值（例如所有状态 $V_0 = 0$）；
2. 用式 4.2，基于 $V_0$ 算出 $V_1$；
3. 再基于 $V_1$ 算出 $V_2$……如此重复；
4. 当每轮更新量足够小时停止，此时 $V_k \approx v_\pi$。

---

## 六、示例：状态迁移确定时的简化（式 4.3）

当状态迁移是**确定性**的——在状态 $s$ 执行动作 $a$，下一个状态 $s'$ 唯一确定，即 $s' = f(s,a)$——那么对 $s'$ 的求和只剩一项，$p(s'|s,a)$ 退化掉，式 4.2 简化为：

$$V_{k+1}(s) = \sum_{a} \pi(a|s)\,\big\{\,r(s,a,s') + \gamma\, V_k(s')\,\big\}, \qquad s' = f(s,a) \tag{4.3}$$

书中 4.1.2 节"有两个方格的网格世界"就是用式 4.3 做的演算：策略 $\pi$ 是各 0.5 概率左右移动，从 $V_0 = 0$ 开始反复更新，$V_k$ 会一步步逼近第 3 章用联立方程解出的精确值——正是第四节结论的实地验证。

---

## 七、小结

| | 式 4.1 贝尔曼方程 | 式 4.2 更新式 |
|---|---|---|
| **性质** | 恒等式（真值满足的自洽关系） | 迭代规则（逼近真值的算法） |
| **符号** | 小写 $v_\pi$（真值） | 大写 $V_k$（估计值） |
| **角色** | 要解的"方程" $v = f(v)$ | 解方程的"套路" $V_{k+1}=f(V_k)$ |

- **式 4.2 从哪来**：既然 $v_\pi$ 是方程 $v = f(v)$ 的解（不动点），就用不动点迭代 $V_{k+1} = f(V_k)$ 反复逼近它。
- **式 4.2 为何成立（收敛）**：$\gamma < 1$ 让每轮更新把误差压缩 $\gamma$ 倍（压缩映射），故 $V_k$ 从任意初值都指数级收敛到 $v_\pi$。
- **一句话**：贝尔曼方程是"目标"，式 4.2 是"用估计改进估计、自己逼近自己"的自举法达成目标的手段。
