# unchain() / detach() 详解 —— 计算图与梯度隔离

> 出处：《深度学习入门 4：强化学习》第 9 章 Actor-Critic
> 参考代码：`参考资料/deep-learning-from-scratch-4-cn-master/ch09/actor_critic.py`
> 关联笔记：[[笔记_9.1_基于策略的方法与目标函数]]

---

## 一、先搞懂：什么是"计算图"？

### 1.1 一个最简单的例子

假设我们要算 $y = (a + b) \times c$，取 $a=2, b=3, c=4$：

```python
a = 2
b = 3
c = 4
d = a + b        # d = 5
y = d * c        # y = 20
```

在**纯 Python**里，`y` 就是数字 20，仅此而已。

但在 dezero / PyTorch 里，当变量是 `Variable`（张量），框架会**偷偷记账**，画出这么一张图：

```
    a ─┐
       ├──[+]── d ──[×]── y
    b ─┘              │
                      c ─┘
```

这张图叫**计算图（Computational Graph）**。它记录了：**每个变量是怎么算出来的、它的"父节点"是谁**。

### 1.2 为什么要记这张图？—— 为了求导

深度学习的核心是"给我一个 loss，我要知道每个参数应该怎么调"。这就是**反向传播**。

有了这张图，框架就能**从终点反着走**，用链式法则一路算出每个变量的梯度：

$$\frac{\partial y}{\partial a} = \frac{\partial y}{\partial d} \cdot \frac{\partial d}{\partial a} = c \cdot 1 = 4$$

`y.backward()` 触发的就是这个反向遍历过程。**没有这张图，就没有梯度。**

---

## 二、`unchain()` / `detach()` 做了什么？

**一句话**：**斩断变量与它父节点的连接**，让这个变量变成一个"独立的数值"，反向传播走到它这里就停下。

```python
y = d * c
d.unchain()   # dezero
# d.detach()  # PyTorch 等价写法
```

图变成这样：

```
    a       (断开！)

    b       d ──[×]── y
                │
                c ─┘
```

现在 `y.backward()` 反向传播时：
- ✅ 能算出 `y` 对 `c` 的梯度
- ✅ 能算出 `y` 对 `d` 的梯度
- ❌ **算不到 `a` 和 `b`**——因为链子断了

`d` 从"计算出来的中间量"变成了"孤立的常数"。

### 2.1 dezero vs PyTorch 写法对照

| dezero | PyTorch | 效果 |
|---|---|---|
| `x.unchain()` | `x.detach()` | 返回一个断开计算图的新 tensor（原 tensor 不变） |
| `x.unchain_()` | `x.detach_()` | **原地**断开（直接改 x 本身） |
| —— | `with torch.no_grad():` | 在这个上下文里所有操作都不建图 |

> 名字不同，本质一模一样。以下用 `detach` 统一代指两者。

---

## 三、为什么要 detach？—— 4 个典型场景

### 3.1 场景 1：Actor-Critic 里的 TD 目标

```python
target = reward + gamma * self.v(next_state)  # target 依赖 V 网络
v = self.v(state)
loss_v = (v - target) ** 2
loss_v.backward()
```

**问题**：`target` 里也含有 V 网络的输出，反向传播时梯度会**从两条路**流回 V 网络：

```
loss_v ──┬──→ v = V(state)         ← 想要 ✅
         └──→ V(next_state)        ← 不想要 ❌
```

**后果**：V 网络会"作弊"——它可以通过降低 target 来让 loss 变小，而不是真正学准。**训练目标扭曲了**。

这是最经典的"**移动的靶子**"问题。

**修复**：

```python
target.unchain()   # 把 target 钉成一个固定的靶子
```

### 3.2 场景 2：DQN 里的目标网络（同样的问题，同样的解法）

```python
q = self.qnet(state)[action]                          # 当前 Q 值
next_q = self.qnet_target(next_state).max()           # 用目标网络算下一步 Q
target = reward + gamma * next_q
loss = (q - target) ** 2
```

在 PyTorch 里通常这么写：

```python
with torch.no_grad():
    next_q = self.qnet_target(next_state).max()
    target = reward + gamma * next_q
```

`torch.no_grad()` 上下文里所有操作都不建图，等价于对 target 做了 detach。

### 3.3 场景 3：GAN 训练判别器 D 时冻结生成器 G

GAN 里有两个网络，训练判别器 D 时：

```python
fake_image = G(noise)             # 生成器生成假图
D_fake = D(fake_image)             # 判别器判断
loss_D = -log(D_real) - log(1 - D_fake)
loss_D.backward()
```

**问题**：`loss_D` 反向传播时，梯度会流过 `D(fake_image)` → 一直流到 `G` 的参数！**但我们这一步只想训练 D，不想动 G**。

**修复**：

```python
fake_image = G(noise).detach()    # 断开 G 的计算图
D_fake = D(fake_image)
loss_D = -log(D_real) - log(1 - D_fake)
loss_D.backward()                  # 现在梯度只到 D，不会传到 G
```

### 3.4 场景 4：Actor-Critic 里的 delta

```python
delta = target - v      # v 是 V 网络算出的
delta.unchain()         # ← 为什么这里也要断？
loss_pi = -F.log(action_prob) * delta
loss_pi.backward()
```

**如果不断**：`loss_pi.backward()` 会让梯度从 delta 流回 V 网络——**用策略的 loss 去更新价值网络**，这会污染 V。

我们希望：
- `loss_v.backward()` → 只更新 V
- `loss_pi.backward()` → 只更新 π

两个 loss 各管各的，**detach 就是隔离阀门**。

---

## 四、一个完整的对比实验

假设：
- $x = 2$（网络参数）
- $y = x^2 = 4$（中间变量）
- $z = y \times 3 = 12$（最终结果）

我们对 $z$ 求 $x$ 的导数。

### 情况 A：不 detach

```python
x = 2 (需要梯度)
y = x ** 2       # y = 4
z = y * 3        # z = 12
z.backward()
# ∂z/∂x = 3 · 2x = 12 ✅
```

反向传播能一路走通：$z \to y \to x$，得到 $\dfrac{\partial z}{\partial x} = 12$。

### 情况 B：对 y detach

```python
x = 2 (需要梯度)
y = x ** 2       # y = 4
y.detach()       # ← 斩断
z = y * 3        # z = 12
z.backward()
# ∂z/∂x = ???
```

反向传播走到 y 就停了，**因为 y 变成"独立常数 4"，跟 x 没关系了**。

结果：$\dfrac{\partial z}{\partial x} = 0$（或者说：算不出来）。

---

## 五、常见误区（新手必踩）

### ❌ 误区 1："detach 会改变数值"

不会！detach 后**数值完全不变**：

```python
y = x ** 2       # y.data = 4
y.detach()
print(y.data)    # 还是 4
```

它只影响**梯度是否能沿着它反向传播**，不影响前向计算。

### ❌ 误区 2："detach 后就不能再用了"

可以用！detach 后的变量仍然是完全正常的张量，能参与后续计算，只是**梯度不再传到它的历史里**。

### ❌ 误区 3："detach 会影响原变量"

要看用法：

```python
# PyTorch
y_new = y.detach()   # 返回新变量，y 本身不变
y.detach_()          # 原地修改 y

# dezero
y.unchain()          # 原地修改 y（这里要小心！）
```

dezero 的 `unchain()` 是**原地操作**，会修改 y 本身。用之前想清楚。

### ❌ 误区 4："能用 detach 就用，反正安全"

**过度 detach 会让网络学不到东西**。原则：

> **只在"这条梯度路径确实不该存在"时才 detach**。

比如 `y = f(x)`，你显然希望 `y.backward()` 能更新 x——这时候乱 detach 就完蛋了。

---

## 六、判断"要不要 detach"的三步心法

遇到一段代码，问自己：

### 第一步：这个变量参与了 loss 计算吗？
不参与 → 不用管（前向变量不影响梯度）

### 第二步：它的"出身"里有哪些网络参数？
把它拆开，画一下计算图，找出所有源头（叶子节点）。

### 第三步：这些源头里，有没有我"这次不想更新"的参数？
- **全都想更新** → 不 detach
- **有不想动的** → 在合适位置 detach，切断那条路径

---

## 七、Actor-Critic 里两处 detach 的最终解释

```python
# ① 训练 V
target = reward + gamma * self.v(next_state)   # ← target 依赖 V
target.unchain()                                # ← 因为算 loss_v 时不能让梯度回流到 V(next_state)
v = self.v(state)
loss_v = (v - target) ** 2

# ② 训练 π
delta = target - v                              # ← delta 依赖 V（因为 v 依赖 V）
delta.unchain()                                 # ← 因为算 loss_pi 时不能让梯度污染 V 网络
loss_pi = -log(action_prob) * delta
```

**两次 unchain，各自的动机不一样**：
- ① 防"**移动靶子**"（V 网络自己训练自己会作弊）
- ② 防"**越权更新**"（策略的 loss 不该动价值网络）

理解了这两点，Actor-Critic 的 update 代码就再也不会看着头大了。

---

## 八、一句话记忆

> **detach = 只前向、不反向。**
> **它把"计算出来的量"降格成"输入进来的数字"。**
