# 6.1.1 式 6.5 解析：MC 方法的“指数移动平均”更新式

> 出处：《深度学习入门 4：强化学习》6.1.1 节（P154）

---

## 〇、式 6.5 长什么样

书里 OCR 丢字了，它的完整形式是：

$$
V'_\pi(S_t) = V_\pi(S_t) + \alpha\Big\{\, G_t - V_\pi(S_t) \,\Big\}
\tag{6.5}
$$

- $V_\pi(S_t)$：**当前**对状态 $S_t$ 的价值估计（旧值）
- $V'_\pi(S_t)$：**更新后**的价值估计（新值）
- $G_t$：本回合从时刻 $t$ 开始实际拿到的收益（式 6.1 的那个采样值）
- $\alpha$：一个固定的小数（如 0.1），书里叫“固定值 $\alpha$”，其实就是**学习率**

一句话：**朝着 $G_t$ 的方向，挪一小步。**

---

## 一、前置知识 1：为什么要“取平均”

价值函数的定义是一个**期望**（式 6.3）：

$$
V_\pi(s) = \mathbb{E}_\pi\big[\,G_t \mid S_t = s\,\big]
$$

期望没法直接算（不知道环境模型），MC 方法的办法是：**多跑几个回合，把实际得到的 $G_t$ 求平均**。

> 就像想知道“骰子的平均点数”，不查概率表，直接掷 1000 次求平均。

所以问题变成：**怎么求平均？** 书里给了两种，式 6.5 是其中第二种。

---

## 二、前置知识 2：两种“平均”

### （1）样本均值 —— 每个数据权重相同

$$
V_n = \frac{G^{(1)} + G^{(2)} + \cdots + G^{(n)}}{n}
$$

直接这样算要把所有历史数据存起来，浪费内存。1.3.2 节推导过它的**增量式**写法（式 1.5）：

$$
V_n = V_{n-1} + \frac{1}{n}\big(G^{(n)} - V_{n-1}\big)
$$

推导只用了一步小学代数——把分子里前 $n-1$ 项之和写成 $(n-1)V_{n-1}$：

$$
V_n = \frac{(n-1)V_{n-1} + G^{(n)}}{n}
    = V_{n-1} + \frac{1}{n}\big(G^{(n)} - V_{n-1}\big)
\tag{1.5}
$$

好处：只需记住 $V$ 和 $n$ 两个数，不用存历史数据。

### （2）指数移动平均 —— 新数据权重更大

把上面的 $\frac{1}{n}$ **换成固定的 $\alpha$**，就得到了式 6.5：

$$
V_n = V_{n-1} + \alpha\big(G^{(n)} - V_{n-1}\big)
$$

**式 6.5 = 式 1.5 把 $\tfrac1n$ 改成 $\alpha$。** 就这一处改动，别无其他。

---

## 三、具体例子：同一批数据，两种平均算出来不一样

假设某个状态 $s$，连续 3 个回合观测到的收益是：

$$
G^{(1)} = 10,\quad G^{(2)} = 0,\quad G^{(3)} = 5
$$

初始估计 $V_0 = 0$。

### 用样本均值（$1/n$）

| 回合 $n$ | 计算 | 结果 |
|---|---|---|
| 1 | $0 + \frac11(10-0)$ | $10$ |
| 2 | $10 + \frac12(0-10)$ | $5$ |
| 3 | $5 + \frac13(5-5)$ | $5$ |

结果 $5$，正好等于 $(10+0+5)/3 = 5$ ✅ —— 确实是“老老实实的平均”。

### 用式 6.5（取 $\alpha = 0.5$）

| 回合 $n$ | 计算 | 结果 |
|---|---|---|
| 1 | $0 + 0.5(10-0)$ | $5$ |
| 2 | $5 + 0.5(0-5)$ | $2.5$ |
| 3 | $2.5 + 0.5(5-2.5)$ | $3.75$ |

结果 $3.75 \neq 5$。**为什么？因为它更看重最近的数据。**

---

## 四、为什么叫“指数”移动平均：把递归拆开看权重

把 $\alpha = 0.5$ 那一列反复代入展开：

$$
V_3 = \underbrace{0.5}_{\alpha}\,G^{(3)} + \underbrace{0.25}_{\alpha(1-\alpha)}\,G^{(2)} + \underbrace{0.125}_{\alpha(1-\alpha)^2}\,G^{(1)} + \underbrace{0.125}_{(1-\alpha)^3}\,V_0
$$

代入数字验证：$0.5\times5 + 0.25\times0 + 0.125\times10 + 0 = 3.75$ ✅

看权重这一行：$0.5,\ 0.25,\ 0.125,\ \dots$ —— **越老的数据，权重按 $(1-\alpha)^k$ 指数衰减**，而且所有权重加起来正好是 1。这就是“指数移动平均”名字的由来（对应书里图 5-16 右图）。

对比一下样本均值：3 个数据的权重都是 $\frac13,\frac13,\frac13$，**一视同仁**。

---

## 五、$\alpha$ 到底在调什么：把式 6.5 读成“误差修正”

把式 6.5 拆成三块看：

$$
V'_\pi(S_t) = \underbrace{V_\pi(S_t)}_{\text{我原来的猜测}} + \alpha \underbrace{\{\, \overbrace{G_t}^{\text{目标}} - V_\pi(S_t) \,\}}_{\text{我猜错了多少（误差）}}
$$

- 误差 > 0 → 我猜低了 → 往上调
- 误差 < 0 → 我猜高了 → 往下调
- $\alpha$ 决定“一次调多少”：
  - $\alpha = 1$：$V' = G_t$，**完全相信这一次的样本**，忘掉所有历史（波动巨大）
  - $\alpha = 0$：$V' = V$，**根本不更新**
  - $\alpha = 0.1$：每次只朝目标挪 10%，慢但稳

> **打靶比喻**：你估计目标在 100 米处，实测是 120 米。$\alpha=0.1$ 就是把估计改成 102 米——不全信这一枪（可能是风吹的），但也承认自己偏低了。

对照式 1.5 里的 $\frac1n$：随着 $n$ 增大，$\frac1n$ 越来越小，更新越来越迟钝，**旧数据把新数据“压住”**；而 $\alpha$ 恒定，永远保持对新数据的敏感度。

---

## 六、为什么 MC 方法要用固定 $\alpha$（非稳态问题）

这是式 6.5 存在的真正理由（5.4.4 节讲得最清楚）：

- 样本均值假设“数据来自**同一个**固定分布”（稳态）→ 数据越多越准。
- 但在策略控制里，**策略每个回合都在改进**，产生 $G_t$ 的概率分布也跟着变 → **非稳态**。
- 此时 10 个回合前用“烂策略”跑出来的 $G_t$，已经不代表现在的价值了。**它们应该被逐渐遗忘**——而这正是指数移动平均在做的事。

> 一句话：**稳态用样本均值，非稳态用指数移动平均。**（书里 1.6 节小结原话）

---

## 七、代码对照（书中源码，一行就是一个式子）

**样本均值版**（`ch05/mc_eval.py:37`）：

```python
self.cnts[state] += 1
self.V[state] += (G - self.V[state]) / self.cnts[state]   # 除以 n → 式 1.5
```

**固定 α 版 = 式 6.5**（`ch05/mc_control.py`，5.4.4 节的“第 2 个修改”）：

```python
alpha = 0.1
self.Q[key] += (g - self.Q[key]) * alpha                  # 乘以 α → 式 6.5
```

差别只有 `/ self.cnts[state]` 换成 `* alpha`，**其余一模一样**。

**TD 版**（`ch06/td_eval.py:23-26`），把目标 $G_t$ 换成 TD 目标：

```python
def eval(self, state, reward, next_state, done):
    next_V = 0 if done else self.V[next_state]
    target = reward + self.gamma * next_V                 # TD 目标 R_t + γV(S_{t+1})
    self.V[state] += (target - self.V[state]) * self.alpha  # 骨架完全来自式 6.5
```

---

## 八、式 6.5 在 6.1.1 整条推导链里的位置

6.1.1 节其实是在“拼装” TD 方法，式 6.5 提供的是**更新的骨架**：

| 来源 | 贡献 | 式子 |
|---|---|---|
| MC 方法（式 6.3 → 6.5） | **怎么更新**：朝目标挪 $\alpha$ 步 | $V \leftarrow V + \alpha(\,\textbf{目标} - V)$ |
| DP 方法（式 6.4 → 6.6 → 6.8） | **目标是什么**：$R_t + \gamma V(S_{t+1})$，且可采样 | $V_\pi(s)=\mathbb{E}[R_t+\gamma V_\pi(S_{t+1})]$ |
| 二者合体 | **TD 方法** | 式 6.9 |

把式 6.5 的目标 $G_t$ 换成式 6.8 的 TD 目标，立刻得到式 6.9：

$$
V_\pi(S_t) \leftarrow V_\pi(S_t) + \alpha\Big\{\, R_t + \gamma V_\pi(S_{t+1}) - V_\pi(S_t) \,\Big\}
\tag{6.9}
$$

这也正是书中图 6-3 的对比：**两个式子的外壳完全相同，只有“目标”不同**。

---

## 九、一句话总结

> **式 6.5 = 增量式求平均（式 1.5）把 $\frac1n$ 换成固定 $\alpha$。**
> 它让旧数据按 $(1-\alpha)^k$ 指数遗忘，从而适应“策略一直在变”的非稳态场景；
> 同时它给出了强化学习万用的更新骨架：**新估计 = 旧估计 + 学习率 × (目标 − 旧估计)**。

相关笔记：[6.1.1_TD方法推导_式6.6到式6.8.md](6.1.1_TD方法推导_式6.6到式6.8.md)
