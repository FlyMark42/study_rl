# 4.3.1 式4.6 / 4.7 为什么成立 —— 从零讲解版

> 从零开始搭台阶：先把 4 个前置概念讲清楚（每个配具体例子），再水到渠成回答"式4.6/4.7 为什么成立"。
> 进阶版（策略改进定理证明直觉）见 [[4.3.1_式4.6与4.7为什么成立_策略改进定理]]。

---

## 🧱 前置知识 1：策略 μ 是什么

**策略（policy）** = "在每个状态该做什么动作"的行动指南。

- **确定性策略**：一个状态对应**唯一一个**动作，记作 `μ(s) = a`。
- 例：网格世界里 `μ(格子A) = 右`、`μ(格子B) = 上`……像一张贴满箭头的地图。

> 式4.6/4.7 讨论的就是**确定性策略**：给一个状态，吐出一个动作。

---

## 🧱 前置知识 2：两个价值函数 v 和 q（关键）

价值 = "从这里开始，未来总共能拿多少奖励（的期望）"。分两种：

### ① 状态价值函数 `v_μ(s)`
"我在状态 s，**完全按策略 μ 走**，未来能拿多少。"
> 关键词：**从头到尾都听 μ 的**。

### ② 行动价值函数 `q_μ(s, a)`
"我在状态 s，**第一步偏偏先走动作 a**（哪怕 a 不是 μ 推荐的），**从第二步起再乖乖听 μ 的**，未来能拿多少。"
> 关键词：**第一步我说了算，之后交给 μ**。

**具体例子**（网格世界，某状态 s，折扣 γ=0.9）：

| 动作 | 含义 | `q_μ(s, a)` |
|------|------|------|
| 上 | 撞墙，原地 | 3 |
| 下 | 走向陷阱 | 1 |
| 左 | 绕远 | 5 |
| 右 | 靠近目标 | **8** |

假如老策略 `μ(s) = 左`，那么：

$$v_\mu(s) = q_\mu(s, \text{左}) = 5$$

即"按 μ 走"就是"第一步走左，之后也听 μ"——所以 `v` 恰好等于 `q` 中老策略那一项。

> 📌 **桥梁关系**：`v_μ(s) = q_μ(s, μ(s))`

---

## 🧱 前置知识 3：q 和 v 的展开关系（贝尔曼方程）

`q_μ(s, a)` 拆开看 = "**这一步的即时奖励** + **打折后下一状态的价值**"的期望：

$$q_\mu(s,a) = \sum_{s'} p(s'|s,a)\{\,\underbrace{r(s,a,s')}_{\text{这一步奖励}} + \underbrace{\gamma\, v_\mu(s')}_{\text{未来打折价值}}\}$$

- `p(s'|s,a)`：走了 a 之后跳到各个 s' 的概率。
- 网格世界是**确定性迁移**（走右必到右边），求和里只有一项，很简单。

**具体例子**：s 走"右"必到 s'，即时奖励 0，而 `v_μ(s') = 8.9`：

$$q_\mu(s, \text{右}) = 0 + 0.9 \times 8.9 \approx 8$$

这就是上表里"右=8"的来历。

> 📌 结论：**q 可以由 v 算出来**（有了每个格子的 v，就能反推每个动作的 q）。

---

## 🧱 前置知识 4：什么叫"贪婪化"

**贪婪（greedy）** = 在当前状态，把所有动作的 q 值看一遍，**挑最大的那个**：

$$\text{新动作} = \arg\max_a q_\mu(s, a)$$

上表里 `max` 是"右=8"，所以贪婪化把 s 的策略从"左"改成"右"。
（`arg max` = "取到最大值的那个**动作**"，不是最大值本身。）

---

## ✅ 正式回答：式4.6 / 4.7 为什么成立

### 式4.6：`μ'(s) = arg max_a q_μ(s, a)`

**含义**：拿老策略 μ 算出来的 q 表，对每个状态**挑 q 最大的动作**，拼成新指南 `μ'`。

- 前置3 → q 能算出来；
- 前置4 → 挑最大就叫贪婪化。

所以式4.6 只是把"**用 q_μ 做贪婪化**"写成符号，**天然成立**（它是新策略的**定义式**）。

例：`μ'(s) = arg max{上3, 下1, 左5, 右8} = 右`。

### 式4.7：`μ'(s) = arg max_a Σ p(s'|s,a){r + γ v_μ(s')}`

**和4.6是同一件事**，只是把式4.6里的 `q_μ(s,a)` 用**前置3的展开式**替换掉：

$$\underbrace{q_\mu(s,a)}_{\text{式4.6}} \;=\; \underbrace{\sum_{s'} p(s'|s,a)\{r + \gamma v_\mu(s')\}}_{\text{式4.7}}$$

所以 **4.6 = 4.7，靠的就是前置知识3（q 的定义）**。区别只是：

- **式4.6**：手上有 q 表 → 直接查。
- **式4.7**：手上只有 v 表 + 环境模型 → 现场用 v 把 q 算出来再挑最大。

---

## 🎯 为什么这么改"合理"（策略改进定理一句话）

借助桥梁 `v_μ(s) = q_μ(s, μ(s))`：

$$\max_a q_\mu(s,a) \ge q_\mu(s, \mu(s)) = v_\mu(s)$$

"挑最大"永远 ≥ "老动作那一项"，所以贪婪化后**只会更好，不会更差**。
（详细证明直觉见 [[4.3.1_式4.6与4.7为什么成立_策略改进定理]]）

---

## 一句话总结

> **先理解 v（全程听 μ）和 q（第一步自己定、之后听 μ）→ q 能用"即时奖励+打折未来 v"展开（前置3）→ 对 q 挑最大就是贪婪化（式4.6）→ 把 q 换成它的展开式就是式4.7。二者相等靠 q 的定义，改动有效靠 max ≥ 老动作。**
