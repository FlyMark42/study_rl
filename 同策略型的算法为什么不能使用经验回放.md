“
经验回放不仅可以用于Q学习，也可以用于其他强化学习的算法。但是，能够使用经验回放的仅限于异策略型的算法。同策略型的算法只能使用从现在的策略得到的数据。也就是说，由于不能使用过去收集的经验数据，因此就不能使用经验回放。
”
如何理解上面内容，要求结合具体示例，通俗易懂，言简意赅。



可以把**经验回放（Replay Buffer）**理解成：

> **把过去做题的过程记在错题本里，以后反复拿出来学习。**

但并不是所有强化学习算法都允许这样做。

------

## 一、先理解“异策略”和“同策略”

### ① 异策略（Off-policy）

**现在学习时，可以使用以前别的策略产生的数据。**

例如：

- 去年用“乱走策略”收集了很多经验；
- 今年改成“聪明策略”；
- 依然可以拿去年数据继续学习。

典型：

- Q-learning
- DQN

所以：

> 旧经验不会过期，可以存起来反复利用。

于是就能使用经验回放。

------

### ② 同策略（On-policy）

要求：

> 学习所使用的数据，必须由当前策略产生。

策略一变，旧数据就失效。

典型：

- SARSA
- REINFORCE
- PPO（严格来说每轮更新后旧数据也会失效）

因此：

> 过去的数据不能再用。

自然也无法使用经验回放。

------

# 二、举一个走迷宫的例子

假设机器人走迷宫。

------

## （1）Q-learning（异策略）

第一天：

机器人乱走：

```
A → B → C → 奖励+10
```

把经历存进经验池：

```
(A,B,+0)
(B,C,+0)
(C,终点,+10)
```

第二天：

机器人已经学聪明了。

即使现在策略变了，

仍然可以把第一天的数据重新拿出来学习：

```
(A,B,+0)
(B,C,+0)
(C,终点,+10)
```

甚至重复学习100次。

所以：

```
过去经验
↓
经验池 Replay Buffer
↓
随机抽样
↓
更新Q值
```

没有问题。

------

## （2）SARSA（同策略）

第一天：

ε=0.8

机器人非常喜欢随机探索：

```
A → B → C
```

记录：

```
(A,B)
(B,C)
```

------

第二天：

学得差不多了，

ε=0.1，

机器人已经很少随机走。

此时当前策略下，

在状态 B 可能应该去 D：

```
A → B → D
```

而旧数据中却是：

```
A → B → C
```

这不是当前策略会产生的动作。

如果还拿旧数据学习：

相当于告诉机器人：

> “到了B应该去C。”

但现在的策略实际上已经认为：

> “到了B应该去D。”

这样就会产生矛盾。

因此：

### 旧经验已经不符合当前策略

所以必须丢弃。

不能放进经验池反复训练。

------

# 三、为什么异策略不怕？

因为 Q-learning 更新公式：

Q(s,a)\leftarrow Q(s,a)+\alpha\left[r+\gamma\max_{a'}Q(s',a')-Q(s,a)\right]

这里使用的是

> 下一状态的最大 Q 值

而不是

> 当时实际采取的动作。

所以：

- 当年实际走的是 C；
- 现在认为应该走 D；

都没关系。

Q-learning只关心：

> “在 s' 状态下，最好的动作是什么？”

因此：

**数据来自什么策略并不重要。**

------

# 四、为什么同策略不行？

SARSA更新：

Q(s,a)\leftarrow Q(s,a)+\alpha\left[r+\gamma Q(s',a')-Q(s,a)\right]

其中：

### (a') 是当前策略真正会采取的动作。

如果经验来自旧策略：

```
旧策略：
B → C
```

而当前策略：

```
B → D
```

那么更新目标就错了。

学习方向会被旧策略带偏。

所以：

> 同策略算法要求“边走边学”，只能使用当前策略刚产生的数据。

------

# 五、一句话记忆

### 异策略（Q-learning、DQN）

> **谁产生的数据无所谓，所以能建立经验池，反复学习。**

像：

> 看别人的录像、看自己过去的录像，都能学。

------

### 同策略（SARSA、REINFORCE、PPO）

> **只能学习当前策略刚产生的数据。**

像：

> 驾校规定只能按照现在教练的方法练车，以前错误姿势的视频不能再拿来训练。

------

因此书中这句话可以浓缩为：

> **经验回放的本质是“重复利用旧数据”，而只有异策略算法允许使用旧策略产生的数据，所以经验回放只能用于异策略算法；同策略算法要求数据必须来自当前策略，因此旧经验会失效，无法进行经验回放。**