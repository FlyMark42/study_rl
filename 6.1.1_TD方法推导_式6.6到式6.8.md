# 6.1.1 TD 方法的推导：从式 6.6 推导出式 6.8

> 出处：《深度学习入门 4：强化学习》6.1.1 节

---

## 一、先看这两个式子到底长什么样

**式 6.6（贝尔曼方程的展开形式）：**

$$
V_\pi(s) = \sum_{a} \pi(a\mid s) \sum_{s'} p(s'\mid s,a)\,\Big\{\, r(s,a,s') + \gamma V_\pi(s') \,\Big\}
$$

**式 6.8（写成期望值的形式）：**

$$
V_\pi(s) = \mathbb{E}_\pi\Big[\, R_t + \gamma V_\pi(S_{t+1}) \;\Big|\; S_t = s \,\Big]
$$

> 说明一下符号：
> - $\pi(a\mid s)$：在状态 $s$ 下，策略选择动作 $a$ 的**概率**。
> - $p(s'\mid s,a)$：在状态 $s$ 做动作 $a$ 后，转移到下一个状态 $s'$ 的**概率**。
> - $r(s,a,s')$：这一步拿到的奖励；$\gamma$：折现率；$V_\pi(s')$：下一状态的价值。

你会发现，**这两个式子的内容其实完全一样，只是写法不同**。6.6 是“摊开来写”，6.8 是“压缩成一个期望符号 $\mathbb{E}$”。推导的关键就一句话：**那一大坨求和，本质就是在算期望值。**

---

## 二、核心知识点：期望值的定义

要理解这步推导，只需要记住期望值（平均值）的定义。

假设有一个随机变量 $X$，它可能取的值是 $x_1, x_2, \dots$，对应概率是 $p_1, p_2, \dots$，那么它的期望（平均）就是：

$$
\mathbb{E}[X] = \sum_i p_i \cdot x_i = (\text{每个可能取值} \times \text{它发生的概率})\text{ 再全部加起来}
$$

**一句话：期望 = “概率 × 取值” 的总和。**

> **生活例子**：掷一个骰子，点数 1~6 各有 $\tfrac{1}{6}$ 的概率。
> 平均点数 $= \tfrac{1}{6}\times1 + \tfrac{1}{6}\times2 + \dots + \tfrac{1}{6}\times6 = 3.5$。
> 这里就是把“每个点数 × 它的概率”加起来——这就是期望。

请记住这个形状：**`Σ (概率 × 某个值) = 期望`**。下面我们就是反着用它——看到这个形状，就把它收回成 $\mathbb{E}$。

---

## 三、一步步把 6.6 变成 6.8

### 第 1 步：看清 6.6 里在对“什么”求和

式 6.6 里有两层求和：
- 外层 $\sum_a \pi(a\mid s)$：对所有可能的**动作** $a$ 求和；
- 内层 $\sum_{s'} p(s'\mid s,a)$：对所有可能的**下一状态** $s'$ 求和。

而每一项里被加权的“值”是花括号里的东西：

$$
\Big\{\, r(s,a,s') + \gamma V_\pi(s') \,\Big\}
$$

### 第 2 步：把两个概率合并成“一件事发生的概率”

注意外层乘的是 $\pi(a\mid s)$，内层乘的是 $p(s'\mid s,a)$。把它们乘在一起：

$$
\pi(a\mid s)\cdot p(s'\mid s,a)
$$

这代表什么？它就是 **“在状态 $s$ 下，先选了动作 $a$、又转移到了状态 $s'$” 这一整件事发生的联合概率**。

也就是说，从状态 $s$ 出发，会发生的“剧情”有很多种 $(a, s')$ 组合，每一种组合发生的概率正是 $\pi(a\mid s)\,p(s'\mid s,a)$。

### 第 3 步：套进期望的定义

现在我们把 6.6 重新整理一下，把两层求和合并看待：

$$
V_\pi(s) = \sum_{a,\,s'} \underbrace{\pi(a\mid s)\,p(s'\mid s,a)}_{\text{这种剧情发生的概率}} \;\times\; \underbrace{\Big\{ r(s,a,s') + \gamma V_\pi(s') \Big\}}_{\text{这种剧情下对应的值}}
$$

对照第二节那个形状 **`Σ (概率 × 值) = 期望`**：

- “概率” = $\pi(a\mid s)\,p(s'\mid s,a)$
- “值” = $r(s,a,s') + \gamma V_\pi(s')$

完美吻合！所以这整个求和**就是一个期望**：

$$
V_\pi(s) = \mathbb{E}_\pi\Big[\, r(s,a,s') + \gamma V_\pi(s') \,\Big]
$$

### 第 4 步：换成随机变量的写法，得到 6.8

最后只是把符号换成书里统一的随机变量记号：
- 这一步的奖励 $r(s,a,s')$ 是个随机量，记作 $R_t$；
- 下一个状态 $s'$ 是个随机量，记作 $S_{t+1}$，于是 $V_\pi(s')$ 写成 $V_\pi(S_{t+1})$；
- 再注明“当前已经处在状态 $s$”这个条件，即 $\mid S_t = s$。

代入后就得到了**式 6.8**：

$$
V_\pi(s) = \mathbb{E}_\pi\Big[\, R_t + \gamma V_\pi(S_{t+1}) \;\Big|\; S_t = s \,\Big]
$$

推导完成。✅

---

## 四、一句话总结这步推导

> **6.6 是“手动把所有可能情况的（概率 × 值）一项项加起来”；6.8 是把这个加法用一个期望符号 $\mathbb{E}$ 打包起来。两者数学上完全相等，只是 6.8 更简洁。**

---

## 五、为什么书里要费劲做这一步？（动机）

这才是重点，它解释了 TD 方法的来历：

- **式 6.6（DP 方法用）**：要算那一大坨求和，**必须事先知道** $\pi$、$p$、$r$ 这些环境的“内部规则”（即环境模型）。现实中我们往往**不知道** $p(s'\mid s,a)$ 是多少。
- **式 6.8（TD 方法用）**：写成期望 $\mathbb{E}[\,R_t + \gamma V_\pi(S_{t+1})\,]$ 后，我们就**不必真的去算这个期望**了。期望可以用**采样**来近似——智能体实际走一步，得到一个真实的 $R_t$ 和真实的下一个状态 $S_{t+1}$，用这一个样本 $R_t + \gamma V_\pi(S_{t+1})$ 去近似那个期望即可。

这正是 6.6 → 6.8 这步的意义：**把“需要知道全部概率才能算的求和”，改写成“可以靠实际走一步采样来近似的期望”**，从而摆脱对环境模型的依赖。顺着这个思路，下一步（式 6.9）就用采样值朝着这个“TD 目标”去更新价值函数：

$$
V(S_t) \leftarrow V(S_t) + \alpha\Big\{\, R_t + \gamma V(S_{t+1}) - V(S_t) \,\Big\}
$$

这就是 TD 方法的更新公式了。
