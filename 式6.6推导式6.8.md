# 式 6.6 如何推导出式 6.8？

> 章节：6.1 使用 TD 方法评估策略 → 6.1.1 TD 方法的推导

---

## 一、先看两个式子长什么样

**式 6.6**（贝尔曼方程，DP 方法用的形式）：

$$V_\pi(s) = \sum_{a,\,s'} \pi(a\mid s)\, p(s'\mid s,a)\,\bigl\{\,r(s,a,s') + \gamma V_\pi(s')\,\bigr\}$$

**式 6.8**（期望值的形式，TD 方法用的）：

$$V_\pi(s) = \mathbb{E}_\pi\bigl[\,R_t + \gamma V_\pi(S_{t+1}) \,\big|\, S_t = s\,\bigr]$$

看上去差别很大，其实是**同一件事的两种写法**。

---

## 二、关键钥匙：期望值的定义

回忆一下概率论里期望的最基本定义：

$$\mathbb{E}[X] = \sum_x P(x)\cdot x$$

也就是说：**「期望」= 所有可能的值 × 对应的概率，然后加起来**。

记住这一点，下面的推导就一目了然了。

---

## 三、一步一步推导

### 第 1 步：识别"随机变量"和"概率"

在式 6.6 里，谁是随机的？两个东西：

- **行动 a** 是按照策略 π 随机选的，概率是 $\pi(a\mid s)$
- **下一个状态 s'** 是环境随机给的，概率是 $p(s'\mid s, a)$

被计算的"值"（也就是 X）是：

$$X = r(s,a,s') + \gamma V_\pi(s')$$

也就是「这一步的奖励 + 折现后的下一状态价值」。

### 第 2 步：合并联合概率

在状态 s 下，**同时**发生「选了行动 a」并且「转移到 s'」的联合概率是：

$$P(a, s' \mid s) = \pi(a\mid s)\cdot p(s'\mid s, a)$$

（先按策略选 a，再按环境转到 s'，两步相乘）

### 第 3 步：套上期望的定义

把式 6.6 里的求和重新理解：

$$\underbrace{\sum_{a,\,s'} \pi(a\mid s)\, p(s'\mid s,a)}_{\text{联合概率 } P(a,s'\mid s)}\cdot \underbrace{\bigl\{r(s,a,s') + \gamma V_\pi(s')\bigr\}}_{\text{被求期望的值 } X}$$

这正是「概率 × 值，再求和」——**就是期望的定义**！所以可以直接写成：

$$V_\pi(s) = \mathbb{E}\bigl[\,r(s,A,S') + \gamma V_\pi(S')\,\big|\, S_t = s\,\bigr]$$

### 第 4 步：换成书里的记号

书里用大写字母表示随机变量：

- 奖励 $r(s,A,S')$ 记作 $R_t$
- 下一状态 $S'$ 记作 $S_{t+1}$
- 下标 π 表示"在策略 π 下取期望"

得到：

$$\boxed{V_\pi(s) = \mathbb{E}_\pi\bigl[\,R_t + \gamma V_\pi(S_{t+1}) \,\big|\, S_t = s\,\bigr]} \quad (6.8)$$

---

## 四、用一个具体例子帮你直观理解

假设在状态 s 下，只有 2 个动作、每个动作只有 2 个可能的下一状态，共 4 种组合：

| 动作 a | 下一状态 s' | π(a\|s) | p(s'\|s,a) | 联合概率 | 即时奖励 r |
|--------|------------|---------|------------|---------|-----------|
| a₁     | s'₁        | 0.5     | 0.8        | 0.40    | 1         |
| a₁     | s'₂        | 0.5     | 0.2        | 0.10    | 0         |
| a₂     | s'₁        | 0.5     | 0.3        | 0.15    | 2         |
| a₂     | s'₂        | 0.5     | 0.7        | 0.35    | -1        |

**用式 6.6 算**（把表格里"联合概率 × (r + γV(s'))"全加起来）：

$$V_\pi(s) = 0.40\cdot(1+\gamma V(s'_1)) + 0.10\cdot(0+\gamma V(s'_2)) + 0.15\cdot(2+\gamma V(s'_1)) + 0.35\cdot(-1+\gamma V(s'_2))$$

**用式 6.8 算**（这就是 4 个可能结果的加权平均，也就是期望）：

$$V_\pi(s) = \mathbb{E}[R_t + \gamma V(S_{t+1})]$$

**两者算出来的数完全相同**！期望符号只是把那一长串求和"打包"成了一个简洁的写法。

---

## 五、为什么要做这个改写？（很重要！）

这一步看似只是换了写法，但它是从 DP 方法走向 TD 方法的**关键转折点**：

| 形式 | 怎么算 | 谁能用 |
|------|--------|--------|
| **式 6.6**（求和形式）| 必须知道 π 和 p 的所有取值，把每一种情况都算一遍 | DP 方法（需要环境模型）|
| **式 6.8**（期望形式）| 既可以解析地算（DP），也可以**用采样数据近似**（MC/TD）| TD 方法（不需要环境模型）|

因为期望可以"用样本平均来逼近"——智能体在环境里实际走一步，就拿到了 $R_t$ 和 $S_{t+1}$ 的一个**样本**，这就是 TD 方法 6.9 式的来源：

$$V(S_t) \leftarrow V(S_t) + \alpha\bigl\{\underbrace{R_t + \gamma V(S_{t+1})}_{\text{用样本近似期望}} - V(S_t)\bigr\}$$

---

## 一句话总结

> 式 6.6 → 式 6.8 是把"枚举所有可能"的写法改成了"求期望"的写法，目的是为了让我们后面可以**用一次采样去近似它**，从而不需要环境模型。这就是 TD 方法的精髓！
