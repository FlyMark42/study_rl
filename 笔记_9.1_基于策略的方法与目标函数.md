# 9.1 基于策略的方法与目标函数

> 出处：《深度学习入门 4：强化学习》第 9 章
> 参考代码：`参考资料/deep-learning-from-scratch-4-cn-master/ch09/simple_pg.py`

---

## 一、为什么需要"基于策略的方法"？

### 1.1 两类方法的本质区别

| | 基于价值的方法 | 基于策略的方法 |
|---|---|---|
| 学什么 | 学"每个动作有多好"（Q 值） | 直接学"该选什么动作"（动作概率） |
| 如何选动作 | 算出 Q 值后 → 选最大的（argmax） | 神经网络直接输出概率 → 按概率采样 |
| 策略类型 | 确定性策略（间接得出） | 随机策略（直接表达） |
| 代表算法 | Q-learning、DQN | REINFORCE、Actor-Critic、PPO |

### 1.2 一个通俗的比喻：去餐厅点菜

- **基于价值**：先给菜单上每道菜打分（红烧肉 8.5、宫保鸡丁 9.0…），再选分数最高的。**要先评估，再决策**。
- **基于策略**：训练一个"点菜直觉"——看到菜单直接告诉你"宫保鸡丁 60%、红烧肉 30%、麻婆豆腐 10% 概率选"。**跳过打分，直接出答案**。

### 1.3 为什么还需要基于策略的方法？

#### ① 连续动作空间，基于价值"算不过来"

机器人手臂转角可以是 0°~360° 之间任意实数，动作有无穷多个。

- 基于价值：需要对每个动作算 Q(s,a)，再 argmax → **无穷多个动作没法枚举**
- 基于策略：网络直接输出"转 35.7°"这种数值 → **天然支持**

> 这是策略梯度最重要的应用场景：机器人控制、自动驾驶等。

#### ② 最优策略本身可能就是"随机的"

石头剪刀布：如果你的策略是确定的（永远出石头），对手立刻就赢了。**最优策略恰恰是 1/3、1/3、1/3 的随机出招**。

- 基于价值 + argmax → 只能输出确定性策略
- 基于策略 → 输出概率分布，可表达均衡的随机策略

#### ③ 收敛更平滑、更稳定

- Q-learning 中，Q 值轻微变化就可能让 argmax 选的动作**整个翻转**（突变）
- 策略梯度对参数 θ 做**连续微调**，动作概率平滑变化 → 训练更稳定

#### ④ 有时候"策略"比"价值"更简单

例子："看到红灯就停"——策略一句话就说完了，但要算"红灯状态下停的价值 vs 走的价值"反而复杂。**直接学策略可能更省力**。

### 1.4 结合代码看（simple_pg.py）

```python
class Policy(Model):
    def forward(self, x):
        x = F.relu(self.l1(x))
        x = F.softmax(self.l2(x))   # 直接输出动作概率分布
        return x
```

- 网络的输出**就是策略本身**（softmax 概率），不是 Q 值
- `get_action` 按概率**采样**动作（不是 argmax）：
  ```python
  action = np.random.choice(len(probs), p=probs.data)
  ```
- 更新时用 `loss = -log(prob) * G`：**回报 G 大的动作 → 提高它的概率**（梯度上升）

### 1.5 小结

> **基于价值是"先评估、再选择"，基于策略是"直接学怎么做"。**
> 当动作连续、策略需要随机、或想要更稳定的训练时，基于策略的方法不可或缺。
> 后面 Actor-Critic 会把两者结合，取长补短。

---

## 二、为什么"G 的期望"是目标函数？

### 2.1 G 为什么会"随机变动"

收益 G = R₁ + γR₂ + γ²R₃ + …（一条轨迹的累计回报）

随机性来自**两个源头**：

1. **策略本身是随机的**：π(a|s) 输出的是概率分布，每次按概率采样动作 → 即使同一状态，这次选 A、下次可能选 B
2. **环境也可能是随机的**：状态转移 p(s′|s, a) 不一定确定，比如游戏里掷骰子、地面打滑

所以：**同一个策略 π_θ，跑两个 episode，得到的 G 完全可能不一样**。

### 2.2 为什么不能直接拿 G 当目标？

#### 反例：直接用 G 会怎样

假设你的策略其实"一般"，但某次运气爆棚，G = 500。

- 以 G 为目标 → 算法以为这个策略很好，朝它使劲优化
- 下次运气差，G = 50 → 算法又以为变差了，反向优化
- **目标在反复横跳，根本没法稳定学习** ❌

#### 用期望就稳了

E[G] 表示：**这个策略 π_θ 跑无数次后，G 的平均值**。

- 它**只取决于策略参数 θ**，不受单次运气影响
- 给定 θ，E[G] 就是一个**确定的数**
- 这才是一个**真正能优化的目标函数** ✅

### 2.3 通俗比喻

#### 比喻 1：抛硬币押注

- 每次结果是随机的（这把赢 100、那把输 50）
- 但"**平均每把赚多少**"是确定的（比如 +10 元/把）
- 你优化下注策略时，看的是**长期平均收益**，不是某一次的运气

#### 比喻 2：考试成绩

- 一次模拟考分数 = G（有发挥成分，可能正常 / 失常）
- 你的**真实水平** = E[G]（无限次模拟的平均）
- 学习的目标是提升**真实水平**，而不是某一次刚好考好

### 2.4 数学表达

策略梯度法的目标函数：

$$J(\theta) = \mathbb{E}_{\tau \sim \pi_\theta}[G(\tau)]$$

- τ 是一条轨迹（trajectory）：s₀, a₀, r₁, s₁, a₁, r₂, …
- G(τ) 是这条轨迹的总回报
- 期望是对"所有可能的轨迹"按 π_θ 产生它们的概率加权平均

目标：**找到让 J(θ) 最大的 θ** → 让"平均回报"最大的策略参数。

### 2.5 实际怎么算？—— 蒙特卡洛近似

**期望需要无穷次采样才能精确求出**，现实中算不了。

策略梯度法的做法：**蒙特卡洛近似**

- 跑 N 个 episode，得到 N 个 G 值
- 用这 N 次的平均近似期望

回头看 `simple_pg.py`：

```python
episodes = 3000        # 跑很多次 → 用大量样本近似期望
for episode in range(episodes):
    ...
    agent.update()     # 每个 episode 用这次的 G 更新一次
```

这就是为什么策略梯度**必须跑很多 episode、训练慢、方差大**——它在用有限次采样去近似一个期望。

### 2.6 小结

> **G 是"一次实验的结果"（有运气成分），E[G] 是"策略的真实水平"。**
> 我们优化的是策略本身的好坏，所以目标函数必须是 E[G]，而不是某一次的 G。

这也埋下了后续改进的伏笔：怎么用**更少的样本、更小的方差**去估计这个期望？
→ 引出 **baseline、Actor-Critic、优势函数 A(s,a)** 等改进。

---

## 三、串起来的一张图

```
基于价值                            基于策略
  ↓                                  ↓
学 Q(s,a)                         学 π(a|s)
  ↓                                  ↓
argmax 选动作（确定性）            按概率采样（随机性）
  ↓                                  ↓
适合离散动作                       适合连续/随机/复杂动作
                                     ↓
                                  目标：max J(θ) = E[G]
                                     ↓
                                  用蒙特卡洛采样近似期望
                                     ↓
                                  方差大 → 引出 baseline、Actor-Critic
```
