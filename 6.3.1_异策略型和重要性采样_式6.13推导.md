# 6.3.1 异策略型和重要性采样：式 6.13 如何导出

下面把式 6.13 的来龙去脉，从“为什么需要它”到“怎么一步步推出来”讲清楚，并配一个具体数字例子。

---

## 一、先看式 6.13 长什么样

异策略型 SARSA 的更新式（式 6.13）：

$$
\text{sampling: } A_{t+1} \sim b
$$
$$
Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha\Big\{\,\rho\big(R_t + \gamma Q(S_{t+1}, A_{t+1})\big) - Q(S_t, A_t)\Big\}
$$

其中权重：

$$
\rho = \frac{\pi(A_{t+1}\mid S_{t+1})}{b(A_{t+1}\mid S_{t+1})}
$$

和普通 SARSA（式 6.10）相比，**唯一的区别就是 TD 目标前面多乘了一个 $\rho$**。这个 $\rho$ 是怎么冒出来的？答案就是**重要性采样**。

---

## 二、问题出在哪：两个策略不一样

先回顾“异策略型（off-policy）”的设定，智能体身上有**两套策略**：

- **目标策略 $\pi$**：我们真正想学好、想评估的策略（这里用**贪婪**策略）。
- **行为策略 $b$**：实际拿来在环境里走动、收集数据的策略（这里用 **ε-贪婪**，为了多探索）。

我们的目标是评估 $\pi$，可是手上的数据（尤其是 $A_{t+1}$ 这个动作）却是用 $b$ 采样出来的。**用错了分布采样，期望值就会算偏**——这就是需要修正的根源。

---

## 三、关键：TD 目标其实是一个“期望值”

普通（同策略）SARSA 的更新式（式 6.10）是：

$$
Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha\Big\{\underbrace{R_t + \gamma Q(S_{t+1}, A_{t+1})}_{\text{TD 目标}} - Q(S_t, A_t)\Big\}
$$

这里的 TD 目标 $R_t + \gamma Q(S_{t+1}, A_{t+1})$ 用到了下一步动作 $A_{t+1}$。

**问题是：$A_{t+1}$ 是随机采出来的，不同的 $A_{t+1}$ 会给出不同的 TD 目标。** 我们真正想要的，是当下一步**按目标策略 $\pi$ 来选动作**时，TD 目标的**平均值（期望）**：

$$
\mathbb{E}_{A_{t+1}\sim \pi}\big[\,R_t + \gamma Q(S_{t+1}, A_{t+1})\,\big]
$$

也就是说，TD 目标本质是一个“在 $\pi$ 这个分布下取平均”的期望值。**记住这一点，重要性采样就能直接套上去了。**

---

## 四、套用重要性采样

回顾第 5 章的重要性采样核心公式（式 5.7）。要算分布 $\pi$ 下的期望，但样本来自分布 $b$，可以这样换算：

$$
\mathbb{E}_{x\sim \pi}[f(x)] \;=\; \mathbb{E}_{x\sim b}\!\left[\frac{\pi(x)}{b(x)}\,f(x)\right]
$$

> 直观理解：只要每个样本都乘上权重 $\dfrac{\pi(x)}{b(x)}$，就能“假装”这些样本是从 $\pi$ 里采的。$\pi$ 里更可能出现的动作权重 >1（放大），$\pi$ 里不爱选的动作权重 <1（缩小）。

现在把 $x$ 换成动作 $A_{t+1}$，把 $f(x)$ 换成 TD 目标 $R_t + \gamma Q(S_{t+1}, A_{t+1})$：

$$
\underbrace{\mathbb{E}_{A_{t+1}\sim \pi}\big[R_t + \gamma Q(S_{t+1}, A_{t+1})\big]}_{\text{我们想要的}}
= \mathbb{E}_{A_{t+1}\sim b}\!\left[\frac{\pi(A_{t+1}\mid S_{t+1})}{b(A_{t+1}\mid S_{t+1})}\big(R_t + \gamma Q(S_{t+1}, A_{t+1})\big)\right]
$$

右边的样本就是用**行为策略 $b$** 采的（我们手上正好有这种数据！），只要给 TD 目标乘上那个比值。把这个比值记作 $\rho$：

$$
\rho = \frac{\pi(A_{t+1}\mid S_{t+1})}{b(A_{t+1}\mid S_{t+1})}
$$

于是“用 $b$ 采样、再乘 $\rho$”得到的 TD 目标，就等价于“按 $\pi$ 来评估”的 TD 目标。把它代回式 6.10 的更新式，就得到了式 6.13：

$$
Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha\Big\{\,\rho\big(R_t + \gamma Q(S_{t+1}, A_{t+1})\big) - Q(S_t, A_t)\Big\}
$$

**推导就这么简单：把 TD 目标看成 $\pi$ 下的期望 → 用重要性采样换成 $b$ 下的期望 → 多出一个权重 $\rho$。**

---

## 五、一个具体数字例子

假设智能体走到了状态 $S_{t+1}$，该状态有 4 个动作。两套策略给出的概率为（设贪婪方向是“动作 0”）：

| 动作 | 目标策略 $\pi$（贪婪） | 行为策略 $b$（ε-贪婪, ε=0.1） |
|------|------|------|
| 0（最优） | 1.0 | 0.925 |
| 1 | 0.0 | 0.025 |
| 2 | 0.0 | 0.025 |
| 3 | 0.0 | 0.025 |

实际用 $b$ 采样，假设这次正好采到了 **$A_{t+1}=0$**。又设 $R_t = 1$，$\gamma = 0.9$，$Q(S_{t+1}, 0) = 5$。

**1. 算权重 $\rho$：**
$$
\rho = \frac{\pi(0\mid S_{t+1})}{b(0\mid S_{t+1})} = \frac{1.0}{0.925} \approx 1.08
$$

> 含义：动作 0 在目标策略里比在行为策略里更受青睐，所以**放大**它的贡献（权重 >1）。

**2. 算修正后的 TD 目标：**
$$
\rho\big(R_t + \gamma Q\big) = 1.08 \times (1 + 0.9 \times 5) = 1.08 \times 5.5 \approx 5.94
$$

**再看另一种情况**：如果这次 $b$ 采到的是非贪婪动作，比如 **$A_{t+1}=1$**，那么：

$$
\rho = \frac{\pi(1\mid S_{t+1})}{b(1\mid S_{t+1})} = \frac{0.0}{0.025} = 0
$$

修正后 TD 目标 $= 0 \times (\dots) = 0$。

> 含义：目标策略 $\pi$（贪婪）**根本不会选动作 1**，所以这条经验对评估 $\pi$ 来说“不算数”，权重直接归零、忽略掉。这正是重要性采样在自动帮我们“过滤”掉与目标策略不符的样本。

这两种情况合在一起、在多次采样后取平均，效果就等同于“一直按 $\pi$ 选动作”所得到的 TD 目标——这就是 $\rho$ 存在的意义。

---

## 六、一句话总结

> 式 6.13 = 普通 SARSA（式 6.10）+ 重要性采样权重 $\rho$。
> 因为数据是用行为策略 $b$ 采的，而我们想评估的是目标策略 $\pi$，所以给 TD 目标乘上比值 $\rho = \dfrac{\pi(A_{t+1}\mid S_{t+1})}{b(A_{t+1}\mid S_{t+1})}$ 做“分布校正”，把 $b$ 下的样本“翻译”成 $\pi$ 下的期望。

**补充提醒**：当 $\pi$ 和 $b$ 差异很大时，$\rho$ 会忽大忽小，导致更新不稳定（方差大）。这也是为什么书里随后说——能不用重要性采样就尽量不用，而 Q 学习正是一种**不需要 $\rho$** 的异策略方法（它用 $\max$ 算子直接锁定贪婪动作，绕开了这个问题）。

对应代码在 `参考资料/deep-learning-from-scratch-4-cn-master/ch06/sarsa_off_policy.py`，其中 `rho = self.pi[...]/self.b[...]`、`target = rho * (reward + gamma * next_q)` 就是式 6.13 的直接实现。
