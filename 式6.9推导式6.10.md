# 式 6.9 如何推导出式 6.10？

> 章节：6.2 SARSA → 6.2.1 同策略型的 SARSA

---

## 一、两个式子先放出来对照

**式 6.9**（TD 方法评估 V 函数）：

$$V(S_t) \leftarrow V(S_t) + \alpha\bigl\{R_t + \gamma V(S_{t+1}) - V(S_t)\bigr\}$$

**式 6.10**（SARSA 的更新式，用 Q 函数）：

$$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha\bigl\{R_t + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t)\bigr\}$$

看出来变化了吗？

| 位置 | 式 6.9 | 式 6.10 |
|------|--------|---------|
| 当前对象 | $V(S_t)$ | $Q(S_t, A_t)$ |
| TD 目标里的下一步 | $V(S_{t+1})$ | $Q(S_{t+1}, A_{t+1})$ |

就是把每一处 $V(\cdot)$ 都换成了 $Q(\cdot, \cdot)$，多带了一个**动作参数**。但**为什么可以这样换？为什么要这样换？** 这才是关键。

---

## 二、先搞清楚：V 和 Q 到底有什么区别？

这是理解整个推导的基础。

### 用"骑车去学校"打个比方 🚲

假设你每天早上从家出发去学校，有 3 条路可选：A 路（快但堵）、B 路（绕远但顺畅）、C 路（中等）。每天的"奖励"是"准时到 + 1 分，迟到 - 1 分"。

- **V(家)** = 「从家出发，按照你**平时的习惯**（比如随便选一条路）去学校，平均能得多少分」
  - 这是一个总体平均，不区分你今天选哪条路。

- **Q(家, A 路)** = 「从家出发，今天**固定走 A 路**，之后再按你平时的习惯走，平均能得多少分」
  - 这个值**指明了具体动作**。
  - 同理 Q(家, B 路)、Q(家, C 路) 各自不同。

### 两者的数学关系

$$V(s) = \sum_a \pi(a\mid s) \cdot Q(s, a)$$

**V 是 Q 在所有动作上的加权平均**（按策略 π 的概率加权）。

---

## 三、为什么要从 V 改成 Q？（动机）

回到强化学习的核心目标：**改进策略**（也叫"控制"）。

策略改进通常是这样做的——在状态 s 下，选那个"看起来最好"的动作：

$$\pi'(s) = \arg\max_a \,\text{(动作 a 的好坏)}$$

那么"动作 a 的好坏"用什么来衡量？

- ❌ **用 V(s) 不行**：因为 V(s) 只告诉你"在状态 s 下，按现策略走的总分是多少"，**没有区分动作**。
- ✅ **用 Q(s, a) 才行**：它直接告诉你"在状态 s 下，选动作 a 的好坏"，可以直接比较：

$$\pi'(s) = \arg\max_a Q(s, a)$$

> 💡 **核心原因**：用 V 改进策略需要环境模型（要知道转移概率 p 和奖励 r 去算每个动作有多好）；用 Q 改进策略**不需要环境模型**，直接比较 Q 值即可。这正是 TD 学习想要的特性！

---

## 四、一步一步推导

### 回顾：V 的贝尔曼方程

上一题我们推过：

$$V(s) = \mathbb{E}\bigl[R_t + \gamma V(S_{t+1}) \mid S_t = s\bigr]$$

含义：当前状态的价值 = 「这一步奖励 + 折现后下一状态价值」的期望。

### 类比：Q 的贝尔曼方程

把"状态 s"换成"状态-动作对 (s, a)"，照葫芦画瓢：

$$Q(s, a) = \mathbb{E}\bigl[R_t + \gamma Q(S_{t+1}, A_{t+1}) \mid S_t = s,\ A_t = a\bigr]$$

含义：在 (s, a) 这对组合下的价值 = 「这一步奖励 + 折现后下一组合 $(S_{t+1}, A_{t+1})$ 的价值」的期望。

> 这里 $A_{t+1}$ 是**按策略 π 在状态 $S_{t+1}$ 选出的下一个动作**。

### 然后采用和 6.9 一模一样的"TD 更新思路"

TD 方法的核心思想是（无论 V 还是 Q）：

> **新估计 ← 旧估计 + 学习率 × (TD 目标 - 旧估计)**

对 Q 套用：

- 旧估计 = $Q(S_t, A_t)$
- TD 目标 = $R_t + \gamma Q(S_{t+1}, A_{t+1})$（用一次采样近似期望）

得到：

$$\boxed{Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha\bigl\{R_t + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t)\bigr\}} \quad (6.10)$$

**式 6.10 就这样推出来了**——它是 V 版 TD（6.9）在 Q 函数上的"翻译"。

---

## 五、配上具体示例：抓宝藏的智能体 🎮

假设有个简单的 1 维网格游戏：

```
[s1] - [s2] - [s3 宝藏 ✨]
```

- 动作：向左、向右
- 折现率 $\gamma = 0.9$，学习率 $\alpha = 0.5$
- 拿到宝藏奖励 = +1，其他时刻奖励 = 0

### 假设当前的 Q 表（初始）：

| 状态 \ 动作 | 左   | 右   |
|------------|------|------|
| s1         | 0.0  | 0.0  |
| s2         | 0.0  | 0.5  |
| s3         | —    | —    |

### 智能体走了一步：

$$S_t = s_1,\quad A_t = \text{右},\quad R_t = 0,\quad S_{t+1} = s_2,\quad A_{t+1} = \text{右}$$

（这就是 **S-A-R-S-A** 五元组的来源 😎）

### 套用式 6.10：

**第 1 步：取出旧估计**

$$Q(s_1, \text{右}) = 0.0$$

**第 2 步：算 TD 目标**

$$R_t + \gamma Q(S_{t+1}, A_{t+1}) = 0 + 0.9 \times Q(s_2, \text{右}) = 0 + 0.9 \times 0.5 = 0.45$$

**第 3 步：算 TD 误差**

$$\text{TD 误差} = 0.45 - 0.0 = 0.45$$

**第 4 步：更新**

$$Q(s_1, \text{右}) \leftarrow 0.0 + 0.5 \times 0.45 = 0.225$$

### Q 表更新后：

| 状态 \ 动作 | 左   | 右      |
|------------|------|---------|
| s1         | 0.0  | **0.225** ⬆️ |
| s2         | 0.0  | 0.5     |
| s3         | —    | —       |

**直观理解**：智能体发现"在 s1 选'右'之后，下一步在 s2 选'右'的价值是 0.5（不错！）"，所以**它把这个'好消息'往回传一格**，把 Q(s1, 右) 从 0 提高到 0.225。

> 这就是 TD 学习的精髓：**用后一步的估计去更新前一步的估计**，让"好消息"和"坏消息"一步步往回流。

---

## 六、为什么这个算法叫 "SARSA"？

更新一次 Q 值需要 5 个数据：

$$\underbrace{S_t}_{\text{S}}\ \underbrace{A_t}_{\text{A}}\ \underbrace{R_t}_{\text{R}}\ \underbrace{S_{t+1}}_{\text{S}}\ \underbrace{A_{t+1}}_{\text{A}}$$

正好是 **S-A-R-S-A** 5 个字母。所以这个算法叫 **SARSA**！是不是很形象 😄

---

## 七、一句话总结

> 式 6.9 → 式 6.10 是把 TD 更新的对象从"状态价值 V(s)"换成了"状态-动作价值 Q(s, a)"。
>
> **本质**：用同样的 TD 思想（旧估计 + 学习率 × TD 误差），只是把更新对象"细化到了每个动作"，这样就可以不依赖环境模型，**直接通过比较 Q 值来改进策略**。
